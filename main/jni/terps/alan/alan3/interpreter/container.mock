#include <cgreen/cgreen.h>
#include <cgreen/mocks.h>

#include "Container.h"

/* DATA */
ContainerEntry *containers; /* Container table pointer */


/* FUNCTIONS */
int containerSize(int container, ATrans trans) { return (int)mock(); }
bool passesContainerLimits(Aint container, Aint addedInstance) { return (bool)mock(); }
void describeContainer(int container) { mock(); }
void list(int cnt) { mock(); }

