OBJECT ATTRIBUTES
    global_object_attribute.

LOCATION ATTRIBUTES
    global_location_attribute.

CONTAINER container_id
END CONTAINER container_id.

OBJECT object_id
    IS
	local_object_attribute.
END OBJECT object_id.

LOCATION location_id
    IS
	local_location_attribute.
END LOCATION location_id.

EVENT event_id
    "This is EVENT_ID happening."
END EVENT event_id.

START AT location_id.

-- 'EMPTY' what optional_where '.'
    EMPTY OBJECT.
    EMPTY LOCATION.
    EMPTY object_id.
    EMPTY unknown_id.
    EMPTY location_id.
    EMPTY container_id.

    EMPTY OBJECT HERE.
    EMPTY LOCATION HERE.
    EMPTY object_id HERE.
    EMPTY unknown_id HERE.
    EMPTY location_id HERE.
    EMPTY container_id HERE.

    EMPTY OBJECT AT LOCATION.
    EMPTY LOCATION AT LOCATION.
    EMPTY object_id AT LOCATION.
    EMPTY unknown_id AT LOCATION.
    EMPTY location_id AT LOCATION.
    EMPTY container_id AT LOCATION.

    EMPTY OBJECT AT hero.
    EMPTY LOCATION AT hero.
    EMPTY object_id AT hero.
    EMPTY unknown_id AT hero.
    EMPTY location_id AT hero.
    EMPTY container_id AT hero.

    EMPTY OBJECT AT location_id.
    EMPTY location_id AT location_id.
    EMPTY object_id AT location_id.
    EMPTY unknown_id AT location_id.
    EMPTY location_id AT location_id.
    EMPTY container_id AT location_id.

    EMPTY OBJECT AT object_id.
    EMPTY LOCATION AT object_id.
    EMPTY object_id AT object_id.
    EMPTY unknown_id AT object_id.
    EMPTY location_id AT object_id.
    EMPTY container_id AT object_id.

    EMPTY OBJECT AT unknown_id.
    EMPTY LOCATION AT unknown_id.
    EMPTY object_id AT unknown_id.
    EMPTY unknown_id AT unknown_id.
    EMPTY location_id AT unknown_id.
    EMPTY container_id AT unknown_id.

    EMPTY OBJECT IN container_id.
    EMPTY LOCATION IN container_id.
    EMPTY object_id IN container_id.
    EMPTY unknown_id IN container_id.
    EMPTY location_id IN container_id.
    EMPTY container_id IN container_id.
