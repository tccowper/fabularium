syntax
  unlock = unlock (obj).

syntax
  cast_unlock = cast unlock on (obj).

verb unlock
  does
    "Unlocked."
end verb.

verb cast_unlock
  does
    "Cast."
end verb.

location l
end location.

object o at l
end object o.

start at l.
