Syntax r = r.

Verb r
  Does
	Depending On Random 1 To 2 
	= 1 Then "random = 1"
	= 2 Then "random = 2"
	Else "depending on not 1 or 2!" -- This should never happen!
					-- It does in v2.8 since the expression
					-- is re-evaluated for every case
	End Depend.
End Verb r.

The l Isa location
End The l.

start at l.
